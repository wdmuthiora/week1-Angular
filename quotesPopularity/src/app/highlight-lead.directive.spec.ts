import { HighlightLeadDirective } from './highlight-lead.directive';

describe('HighlightLeadDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightLeadDirective();
    expect(directive).toBeTruthy();
  });
});
